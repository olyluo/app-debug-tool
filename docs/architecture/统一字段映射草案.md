# 统一参考字段草案

## 一、定位

这份文档不是强制标准，也不是要求所有设备必须完全按这一套字段落地。

它的定位是：

- 平台内部统一展示和统一处理时使用的一套参考抽象
- 多设备适配时的首版字段基线
- Profile 做原始字段映射时的参考目标

也就是说，这里定义的是“参考字段集”，不是“硬编码标准”。

## 二、适用原则

- 不同设备允许字段缺失
- 不同设备允许命名不同
- 不同设备允许字段拆分或合并
- 不同设备允许增加厂商扩展字段
- 真正的落地以设备画像映射为准，而不是以参考字段表为准

## 三、这份草案解决什么问题

它主要用于回答三件事：

- UI 层统一展示什么字段
- Profile 层把不同设备的原始字段优先映射到哪里
- 后续新增设备时，哪些字段最值得先补齐

## 四、总原则

- UI 只面向参考字段，不直接面向原始 ini 键名
- Profile 负责把设备原始字段映射成参考字段
- 同一个参考字段允许多个候选来源
- 参考字段名保持英文、语义明确、长期稳定
- 如果某些设备不适合映射，允许保留为厂商扩展字段

## 五、参考字段分组

### 1. 设备身份 Identity

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `terminalId` | 终端 ID | `terminalid` |
| `serialNumber` | 设备序列号 | `sn.txt`, `identifyserialno`, `serialno` |
| `deviceModel` | 设备型号 | `model`, `productName`, `hardware_version` |
| `vin` | 车辆 VIN | `vin` |
| `vehicleType` | 车辆类型 | `licenseplateclassify`, `vehicleType` |
| `licensePlateNo` | 车牌号 | `licenseplateno` |
| `licensePlateColor` | 车牌颜色 | `licenceplatecolor`, `plateColor` |

### 2. JT808 平台 Platform

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `jt808Enabled` | 是否启用 JT808 | `808enable` |
| `jt808ProtocolVersion` | 808 协议版本 | `808protocolversion` |
| `cityStandard` | 城标 | `citystandard` |
| `mainServerAddr1` | 第一主服务器地址 | `mainserveaddr1` |
| `mainServerPort1` | 第一主服务器端口 | `mainserveport1` |
| `backServerAddr1` | 第一备用服务器地址 | `backserveaddr1` |
| `backServerPort1` | 第一备用服务器端口 | `backserveport1` |
| `mainServerAddr2` | 第二主服务器地址 | `mainserveaddr2` |
| `mainServerPort2` | 第二主服务器端口 | `mainserveport2` |
| `mainServerAddr3` | 第三主服务器地址 | `mainserveaddr3` |
| `mainServerPort3` | 第三主服务器端口 | `mainserveport3` |
| `registerId1` | 第一平台注册 ID | `registerid1` |
| `registerId2` | 第二平台注册 ID | `registerid2` |
| `registerId3` | 第三平台注册 ID | `registerid3` |
| `authCode1` | 第一平台鉴权码 | `authentedcode1` |
| `authCode2` | 第二平台鉴权码 | `authentedcode2` |
| `authCode3` | 第三平台鉴权码 | `authentedcode3` |

### 3. 网络参数 Network

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `apn` | APN | `apn`, `tparammainserverapnaddr` |
| `apnUser` | APN 用户名 | `tparammainserverusr` |
| `apnPassword` | APN 密码 | `tparammainserverpsw` |
| `agpsServer` | AGPS 服务器地址 | `agpsserver` |
| `agpsPort` | AGPS 端口 | `agpsport` |
| `wifiHotspotEnabled` | Wi‑Fi 热点状态 | 运行态探测 / 脚本状态 |
| `rndisIp` | RNDIS IP | 网络探测结果 |

### 4. 远程控制与短信 RemoteControl

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `smsResponseTimeout` | 短信应答超时 | `tparamsmsresptimeout` |
| `smsRetryCount` | 短信重发次数 | `tparamsmsretrysendns` |
| `smsNumber` | 短信号码 | `tparamsmstelnum` |
| `smsAlarmNumber` | 报警短信号码 | `smstxtalarmtelnum` |
| `monitorTel` | 监控电话号码 | `tparammontelnum` |
| `resetTel` | 复位电话号码 | `tparamresettelnum` |
| `rebuildTel` | 重建电话号码 | `tparamrebuildtelnum` |
| `listenTel` | 监听电话号码 | `tparamlistentelnum` |
| `speakTel` | 通话/点名号码 | `tparamspptelnum` |

### 5. 白名单与音视频 AV

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `avWhitelist` | 音视频白名单 | `avwhitelist` |
| `videoChannelCount` | 视频通道数 | profile 推断 / 运行态探测 |
| `videoMainStreamInfo` | 主码流信息 | 运行态探测 |
| `videoSubStreamInfo` | 子码流信息 | 运行态探测 |
| `videoPreviewSupported` | 是否支持视频预览 | connector + profile 能力 |

### 6. AI / ADAS / DMS / BSD

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `adasEnabled` | ADAS 开关 | `adasenable` |
| `dmsEnabled` | DMS 开关 | 设备原始键 / profile 提取 |
| `bsdEnabled` | BSD 开关 | 设备原始键 / profile 提取 |
| `alarmPolicy` | 报警策略集合 | `terminal_alarm_*` |
| `capturePolicy` | 抓拍策略集合 | `timer_photo.*`, `distance_photo.*`, `speed_photo.*` |
| `thresholdConfig` | 阈值配置集合 | ADAS/DMS/BSD 原始键集合 |

### 7. 串口与外设 SerialAndPeripheral

| 参考字段 | 含义 | 常见原始字段示例 |
|---|---|---|
| `rs485Enabled` | RS485 是否启用 | `rs485funcno1` 非 0 |
| `rs485BaudRate` | RS485 波特率 | `rs485baudrate1` |
| `serialProfile` | 串口功能配置摘要 | `serialport` 段 |
| `simChangeState` | SIM 变化状态 | `simchangestate` |
| `simWarnInterval` | SIM 告警间隔 | `simwarninterval` |

## 六、映射优先级建议

### 文本字段
优先顺序：

1. 运行中的主配置
2. Profile 指定的主数据库字段
3. 备份配置
4. 推断值

### 布尔/能力字段
优先顺序：

1. 运行态探测
2. 主配置字段
3. Profile 推断

## 七、示例

### 示例：车牌号

参考字段：`licensePlateNo`

可能来源：

- `/userdata/product.ini` 的 `licenseplateno`
- 数据库中的车辆基础表
- 平台参数缓存表

### 示例：第一主服务器

参考字段：`mainServerAddr1`

可能来源：

- `/userdata/product.ini` 的 `mainserveaddr1`
- 平台参数库映射
- 远程参数区 `tparammainserveraddr`

### 示例：白名单

参考字段：`avWhitelist`

可能来源：

- `avwhitelist`
- 运行态 JT808 白名单接口返回

## 八、后续落地建议

- 在 `core:device` 中定义参考字段模型
- 在 `profiles` 中实现原始字段到参考字段的映射器
- 在 `feature-config-center` 中优先依赖参考字段模型
- 无法稳定映射的内容，保留在厂商扩展字段区
